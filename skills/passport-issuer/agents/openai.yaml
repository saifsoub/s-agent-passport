interface:
  display_name: "S/Passport Issuer"
  short_description: "Issue and manage verified S/Agent Passports"
  default_prompt: "Use $passport-issuer to process this agent's Passport request end to end."
policy:
  allow_implicit_invocation: true
